# Security Policy

## 凭据

- 密码、token、cookie、SSH 私钥和 SMTP 应用密码不得进入代码、Git、日志或运行 manifest。
- 凭据只能来自环境变量、操作系统凭据存储或 Windows 当前用户范围的 DPAPI 配置。
- 状态文件只能记录掩码标识、凭据来源和是否配置，不能记录明文地址或秘密值。
- 发现疑似泄漏时先停止提交和推送，撤销并轮换凭据，再清理 Git 历史；仅删除当前文件不等于完成处置。

## 外部系统

- 数据上传必须限制到 manifest 声明的远端根目录。
- 自动删除默认禁止。只有服务端 SHA-256 验证通过并获得人工明确确认后，才能单独执行删除流程。
- dashboard 必须默认绑定本机回环地址。涉及凭据配置的接口必须使用短期会话 token，并禁止写入访问日志。

## 报告

安全问题不要写入公开 issue。请通过仓库所有者的私密渠道报告，附受影响文件、commit、发现时间和已采取的隔离措施；报告中同样不要粘贴完整凭据。
